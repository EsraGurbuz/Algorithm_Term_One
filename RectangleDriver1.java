public class RectangleDriver1 {
	public static void main(String[] args){

		//Create a rectangle object (living room)
		Rectangle livingRoom = new Rectangle();
		System.out.println(livingRoom.getLength());
		System.out.println(livingRoom.getWidth());
		System.out.println(livingRoom.getArea());

		/*//Let's assign a value of 10.0 to the length field and 15.0 to the width field
		livingRoom.setLength(10.0);
		livingRoom.setWidth(15.0);

		//Let's print the length, width, and area of the object
		System.out.println(livingRoom.getLength());
		System.out.println(livingRoom.getWidth());
		System.out.println(livingRoom.getArea());

		//Create a rectangle obcejt (bedroom)
		Rectangle bedRoom = new Rectangle();

		//length 0.0
		//width 0.0

		//Let's assign a value of 5.5 to the length field and 8.0 to the width field.
		bedRoom.setLength(5.5);
		bedRoom.setWidth(8.0);

		//Let's print the area
		System.out.println("This is the area of the bedroom " + bedRoom.getArea());
		*/
	}
}